# AGENTS.md

## Cursor Cloud specific instructions

This repository currently contains **no application source code** — only `README.md` and a Python `.gitignore` describing a planned RAG pipeline. There is nothing to install, lint, test, build, or run until code and a dependency manifest are added.

- **Update script:** intentionally a no-op (`true`). When a `requirements.txt`, `pyproject.toml`, or similar lands, update the environment install script accordingly.
- **Services:** none. Do not attempt to start a dev server or run tests against this scaffold.
